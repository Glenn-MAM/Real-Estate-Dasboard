services:
  - type: web
    name: real-estate-dashboard
    env: python
    region: frankfurt
    buildCommand: pip install -r requirements.txt
    startCommand: python real_estate_dashboard.py
    envVars:
      - key: PORT
        value: 10000